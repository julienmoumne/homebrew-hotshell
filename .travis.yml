language: objective-c
before_install:
  - brew update
install:
  - mkdir -p $(brew --repo)/Library/Taps
  - ln -s $PWD $(brew --repo)/Library/Taps/homebrew-hotshell
  - brew tap --repair
script:
  - brew audit hs
  - brew test hs
  - brew install -v hs
